#[derive(Clone, Copy, Component, Debug, Default, Deserialize, Serialize, PrefabData)]
#[prefab(Component)]
#![enable(implicit_some)]

Prefab(
    entities: [
        PrefabEntity(
            data: Food(
                name: "Hamburger",
                skills: Vec<Skill> 
            )
        )
    ]
)